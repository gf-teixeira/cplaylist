#include <stdio.h>
#include <stdlib.h>


struct Node{
    char title[15];
    struct Node *prev;
    struct Node *next;
};

void insert(struct Node** head, char title[15]){

}

void remove(struct Node** head, char title[15]){

}

void play(struct Node* head, int i){

}
int main(){


   // struct Node* head = (struct Node*) malloc(sizeof(struct Node));
    struct Node* head = NULL;

    return 0;
}
